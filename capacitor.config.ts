import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Wind River Web Client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
